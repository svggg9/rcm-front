# Дизайн-система рцмаркет

Рабочий эталон: `/design-system`, только при `npm run dev`. В production маршрут возвращает 404 и не предназначен для публичной навигации. Метаданные запрещают индексацию.

Страница создана 8 сентября 2026. Это место сверки, не автоматическое утверждение всех текущих стилей.

## Последнее состояние к передаче, 19 сентября 2026

Последнее решение 10 сентября: в кабинете продавца единая двухуровневая навигация в SellerSidebar. Верхний ряд: Обзор, Каталог, Заказы, Финансы, Магазин, Данные и документы; ниже — подразделы выбранного раздела. Счётчиков нет. Кнопки добавления товара/подборки расположены у поиска и фильтров. Ранее описанный ниже CabinetPanel не является текущей навигацией этих seller-разделов; компонент сохранён для остальных мест.

Редактор товара — правая панель поверх списка с сохранённой прямой ссылкой; подборка — компактный диалог. Действия закреплены снизу, несохранённые правки защищены подтверждением, на мобильном редактор полноэкранный. Текущий шрифт — Inter из app/layout.tsx. Исторические пометки «только эталон» далее сверять с таблицей внедрения 9 сентября и рабочим кодом.

## Панели с вкладками кабинетов, ранний вариант 10 сентября 2026

Внутренние вкладки и их содержимое объединены в общий `CabinetPanel`: прямоугольная рамка `--line`, серая полоса `--soft`, белая активная вкладка, соединённая с белой областью содержимого. Без подчёркиваний и отдельных чёрных кнопок-вкладок. Основные действия размещаются справа в шапке; поиск, фильтры, таблицы, загрузка и пустые состояния — внутри панели. На узком экране вкладки прокручиваются, действия переходят на следующий ряд. Табы связаны с панелью через ARIA и доступны с клавиатуры.

Общий компонент применяется к товарам/подборкам, заказам/возвратам, финансам и задачам продавца, заказам/возвратам покупателя и существующим вкладкам админки. Пример — `/design-system#lists`. Это основа кабинета как рабочего пространства с таблицами; дальнейшая перестройка остальных разделов остаётся концепцией.

## Актуальная сверка с утверждёнными макетами, 9 сентября 2026

Ниже — текущее состояние переноса. Исторические записи далее относятся к моменту своей правки, а не означают, что весь проект уже приведён к эталону.

| Согласовано | В рабочих экранах |
| --- | --- |
| Отдельная чёрная шапка продавца и горизонтальное текстовое меню под ней, вкладки без счётчиков и подчёркиваний: прозрачный фон у всех пунктов; активный — чёрный текст, остальные — серый `--muted`, при наведении любой пункт — тёмно-золотой `--gold-deep` | Перенесено на вкладки кабинета и редактирование товара. Логотип и личный кабинет, без покупательских поиска, корзины и избранного. «Данные и документы» в общем ряду. На узком экране меню прокручивается, выбранный пункт становится видимым без вертикального скролла страницы |
| Главная: название бренда со статусом «Активен», задачи, магазин, отдельная подготовка, последние события и поддержка | Базовая композиция перенесена; подготовка скрывается после завершения, финансы на главной — доступная выплата, активные товары доступны горизонтально |
| Задачи с названиями-поводами как темы писем; действие отдельно | Подключены конкретные заказы, возвраты и товары на доработке из существующих seller API. Кнопка отдельно от заголовка. «Ответ от поддержки» не выдумываем: для него пока нет источника данных |
| Передать в доставку за 3 суток от оплаты | В задачах и деталке срок равен `paidAt + 72 часа`. Отменённые, завершённые и уже переданные заказы исключены из задач. При отсутствии времени оплаты показываем, что срок неизвестен; дату создания вместо оплаты не используем |
| Бейджи задач и уведомления; фильтры без лишних счётчиков | Счётчики убраны из фильтров заказов и товаров. В верхнем меню продавца счётчики также убраны по уточнению пользователя |
| Деталка заказа: мини-карточки товаров, затем оплата, затем доставка; количество только больше 1 | Перенесено в общий рабочий `SellerOrderCard`, включая покупательское использование. Действия продавца не показываем покупателю |
| Поля, чёрные подписи и значения, ошибки под полями; три уровня кнопок и их состояния; статусы и алерты | Обновлены общие компоненты и стили, размечены нативные поля кабинетов, редактора товара и админки. Общий обработчик прокручивает к ошибке при отправке формы. Полный ручной аудит каждого экрана ещё не завершён |
| Куб / чек / грузовик, размеры 24 / 20 / 32, контур 1,5; без текстовых точек-разделителей и лишних подписей пустых состояний | Три утверждённых образа перенесены в общий `Icon`, служебные стрелки и пустые состояния приведены к размерам. Текстовые точки-разделители удалены из рабочих TS/TSX; маркеры содержательных списков и маска пароля сохранены. Остальные специализированные образы не заменяем без согласования |
| Название «рцмаркет» и более широкий ассортимент для жизни, дома и подарков | Обновлены публичная главная, футер, метаданные, страницы о проекте и заявки продавца, старые отображаемые названия. API, технические идентификаторы и реальные SKU не переименовывались |

Источник сверки: ответы пользователя и сохранённый подробный макет `rcm-seller-home-complete.html`. Дополнительные сценарии, помеченные в макете как предложения, не считаем автоматически утверждёнными. Переключатель «До запуска / Работающий магазин» служил демонстрации состояний — в реальном кабинете состояние определяется данными, переключатель пользователю не добавляем.

Текущая правка меняет оболочку, а не бизнес-логику: маршруты, авторизация, создание товаров и действия с заказами сохранены. Боковой резерв ширины убран также из редактора товара и его фиксированной панели действий. Шапка публичных страниц и страницы заявки продавца сохранена.

Уточнение навигации: «Возвраты» убраны из верхнего меню и доступны только как подвкладка раздела «Заказы». При открытии возвратов верхний пункт «Заказы» остаётся активным. Прямые ссылки `/seller?tab=returns` сохранены.

Проверки оболочки: render-smoke всех шести ссылок и активного раздела (включая возвраты внутри заказов), маршрутизация отдельной шапки, ESLint, TypeScript и `git diff --check`. В браузере проверены главная, заказы, переход к документам с показом активного пункта на ширине 360 px и редактор товара с нижней панелью действий на 1280 px. Данные форм и товаров не изменялись.

## Уже согласовано в обсуждении

- Чёрный, белый, небольшие золотые акценты и прямые углы
- Sans-serif с кириллицей; сохраняем типографику левого варианта сравнения. Подписи полей и пояснения — чёрные, не бледные
- Основная кнопка чёрная, вторичная белая с чёрной рамкой
- Третий уровень — текстовая кнопка без рамки и заливки, при наведении мгновенно появляется бледно-серый фон
- Мягкое нажатие; при загрузке кнопки только спиннер, без видимого текста
- «Сохранить» активно и при незаполненной форме: по нажатию показываем ошибки у полей и прокручиваем к первой ошибке. Во время запроса повторная отправка блокируется
- Недоступное состояние остаётся для действительно недоступных действий с понятной причиной, а не как стандарт для незаполненных форм
- Ошибки с бледной красной заливкой, успех с зелёной, без рамок
- Цветные точки-индикаторы статуса допустимы; точки-разделители в текстах не используются
- Список заказов — визуальная основа для списков кабинета
- Иконки действий внутри текстовых кнопок не добавляем без отдельного согласования

Остальные размеры, палитра оттенков, сетка и состояния требуют сверки. Наличие значения в коде не означает его утверждение.

## Сравнение типографики по референсам Farfetch

На `/design-system#type` показаны одинаковые примеры поиска, текста, поля, общего сообщения об ошибке и кнопок. Слева — общие компоненты как есть; справа — изолированный вариант на том же Inter с выбранными поправками. Это сравнение принципов, не копия Farfetch Basis; чужие шрифтовые файлы не подключались.

Выбор пользователя: типографика и кнопки левого варианта. Сохраняем их размеры и насыщенность. Маленькие подписи полей остаются 12 px, пояснения — 15 px; меняется только их цвет на чёрный, без согласованного увеличения font-weight. Из правого варианта берём только дизайн общего алерта: бледно-розовая заливка без рамки и тёмный текст размером 15 px, как слева, вместо 16 px. Это решение для общего сообщения, не перенос оформления на ошибку под полем. Композиция и ширина двух примеров одинаковые; на мобильном они расположены друг под другом.

История: увеличенный правый вариант с основным текстом и вводом 16 px, подписью 14 px и кнопками 16 px / 700 высотой 48 px отклонён.

Уточнение: в выбранном варианте отступ между названием поля и вводом уменьшен с 8 до 4 px. Пока только в эталоне.

Рамка поля: 9 сентября пользователь усилил её до сплошной чёрной 1 px (`--black`, `#111111`). Это заменяет ранее выбранный средний серый `#808080`. При ошибке красная рамка по всему периметру, включая фокус. Пока только в эталоне.

Поля — уточнения 9 сентября: названия полей и нижние пояснения чёрные; сообщение об ошибке остаётся красным, уменьшено до 13 px / 18 px и находится в 4 px от нижней границы, по её левому краю. Очевидные плейсхолдеры, повторяющие название поля, убраны. Оставляем полезные примеры формата, например «Например, 990022» для почтового индекса; у поиска без видимой подписи подсказка сохраняется. Эти правила объединены в `FieldPreview.module.css` для раздела полей и правого выбранного образца типографики. Левый образец остаётся историческим сравнением. Общие компоненты и рабочие страницы пока не менялись.

Следующее уточнение: текст внутри всех полей унифицирован по обычному вводу — Inter 14 px / 21 px / 400, `--black`. Это относится к вводу, многострочному полю, номеру телефона вместе с +7, выбранному значению и вариантам выпадающего списка. Названия полей оставлены 12 px, но усилены с 300 до 400, поскольку прежнее тонкое начертание выглядело бледным даже при чёрном цвете. Это заменяет прежний выбор 300 для подписей; нижние пояснения не утяжеляем. Применено только к выбранным образцам эталона, не ко всем рабочим формам.

Текст общего алерта «Неверный адрес электронной почты или пароль»: общая роль `textBody`, Inter 15 px / 21 px / 400, чёрный цвет. После примерки облегчённого 300 пользователь попросил более чёткое начертание по референсу; размер не увеличиваем. Применено в изолированных образцах типографики и алертов, без изменения рабочих страниц. Пояснения остаются 300.

Все изменения этого сравнения ограничены страницей эталона: слева сохраняется исходный вариант, справа показаны выбранные поправки. Рабочие страницы, формы, токены и общие компоненты не менялись. Выбор оформления в эталоне не означает разрешения на его глобальное внедрение.

## Что показывает эталон

| Раздел | Источник | Статус |
| --- | --- | --- |
| Цвета и типографика | `app/styles/tokens.css`, `typography.css` | Выбрана типографика слева; чёрные подписи и пояснения показаны только в локальном варианте |
| Кнопки | `Button`, `buttons.css` | Согласованы 9 сентября: три уровня, лайк, состояния и правила доступности; перенос в проект отдельно |
| Поля | `TextInput`, `PhoneInput`, `FormSelect`, `Textarea`, `FieldPreview.module.css` | Общие компоненты с изолированными правками подписей, рамок, ошибок и плейсхолдеров |
| Статусы | `StatusBadge` | Текущие образцы в эталоне согласованы 9 сентября |
| Алерты | `alerts.css` и изолированный предпросмотр | Цветные заливки без рамок согласованы 9 сентября; рабочие алерты пока прежние |
| Списки | `OrderDetailsPreview`, стили строки `SellerOrderCard`, `CabinetTabs` | Табы без счётчиков; изолированный макет деталки в три ряда по референсу пользователя |
| Иконки | `Icon`, `DesignSystemIcon`, `IconOptionsPreview` | Согласованы образы и размеры 20 / 24 / 32, контур 1,5; глобальный перенос отдельно |
| Состояния | `EmptyState`, `ProductTileSkeleton` | Пусто, загрузка, ошибка, успех |

Все интерактивные примеры используют локальное состояние React. Данные не отправляются и не сохраняются. В карточке заказа отключены доставка документов и загрузка деталей; демонстрационных платёжных действий нет.

## Обнаруженные расхождения, пока без глобальных исправлений

- Размеры и веса текста переопределяются на отдельных страницах
- Подписи полей используют 12 px вместо общего `--font-input-label`
- Телефон оформлен иначе, чем обычный ввод
- Общие алерты ещё содержат рамки, хотя цветной предпросмотр без рамок уже согласован
- Статусы различаются между общим бейджем, макетами и карточкой заказа
- В `buttons.css` есть обращения к отсутствующему `--transition`
- Общая строка для заказов, товаров и задач ещё не выделена
- Иконки, контраст, клавиатурный доступ и reduced-motion требуют отдельной полной ревизии

## Порядок согласования

9 сентября 2026 пользователь завершил согласование кнопок. Правило для форм: «Сохранить» остаётся активной до отправки, даже если обязательные поля ещё не заполнены. По нажатию валидируем поля; при ошибках показываем пояснения рядом с ними и прокручиваем к первой ошибке, при корректных данных сохраняем. Во время запроса показываем спиннер и блокируем повторную отправку. Недоступное состояние не удаляем из ДС: оно предназначено для действительно недоступных действий с объяснением причины. Это заменяет раннюю идею делать кнопку сохранения реквизитов недоступной до заполнения всех полей. Сейчас зафиксировано правило; рабочие формы ещё не менялись.

Текстовая кнопка добавлена в эталон по согласованию: третий уровень для действий внутри карточки («Изменить», «Удалить»). Общие размер и шрифт сохранены: высота 42 px, Inter 15 px / 600, горизонтальный отступ 16 px. Без рамки и скругления; фон `--surface-hover` появляется и исчезает без перехода. Мягкое нажатие, видимый клавиатурный фокус, состояния загрузки и недоступности показаны рядом. Использован общий `Button` с вариантом `ghost` и изолированным CSS эталона; существующие глобальные `ghost` и `tertiary` не изменены.

Лайк по референсу пользователя: квадратная зона 44 × 44 px без рамки и скругления, существующие иконки `/icons/like.svg` и `/icons/like-filled.svg` размером 24 px. При наведении бледно-серый фон `--surface-hover` появляется мгновенно; только иконка плавно переходит от контура к чёрной заливке за 220 мс. Клик переключает локальное состояние с `aria-pressed`; после ухода курсора выбранное сердечко остаётся заполненным, фон исчезает. Фокус с клавиатуры видимый, при reduced-motion переход отключён. Пока только пример на `/design-system#buttons`, рабочее избранное не менялось.

Уточнение: при снятии лайка сердечко сразу становится контурным, без перехода, даже если курсор остался над кнопкой. Серый фон сохраняется до ухода курсора. Плавная заливка при наведении снова доступна после выхода и повторного входа курсора.

Пример основной кнопки: исходный текст → спиннер без текста → галочка из общего `Button` на 1,5 секунды → исходный текст. Галочка появляется только после успешного результата; при ошибке остаётся сообщение с возможностью повтора. На время загрузки и показа успеха повторное нажатие заблокировано. В эталоне скрытая копия надписи сохраняет ширину кнопки во всех состояниях; скрытая копия исключена из дерева доступности. Общий компонент пока не менялся.

Итог просмотра 9 сентября: пользователь подтвердил статусы и алерты в эталоне. Не заменяем принятый текущий образец статусов на прежнюю идею точек автоматически. Для табов предложено убрать счётчики — показываем вариант без них, пока без глобального изменения навигации. Список заказов остаётся ориентиром, но его раскрытая деталка отклонена и требует отдельного макета. Первые три иконки («Товар», «Заказ», «Передать в доставку») тоже требуют новых предложений, текущие не считаем утверждёнными.

Пустые состояния: убираем очевидные пояснения под «Товаров пока нет» и «Корзина пуста». По прямой просьбе пользователя подпись «Добавьте вещи из каталога, чтобы оформить заказ» удалена также из рабочего компонента пустой корзины; заголовок и ссылка «Каталог» сохранены. В демонстрации успешного действия «Товар отправлен на модерацию» иконка явно зелёная (`--success`), полезное пояснение о статусе проверки остаётся.

После просмотра внешних референсов пользователь выбрал собственную композицию деталки в три ряда: мини-карточки каждой позиции сверху, затем оплата, затем доставка. В двух нижних рядах заголовки слева, содержимое справа, на мобильном одна колонка. Белый фон, без вложенных серых блоков; тонкие разделители только между основными рядами, как на приложенном референсе. Это направление заменяет предложение автоматически переходить к отдельной странице заказа — новую маршрутизацию не внедряем.

В `/design-system#lists` добавлен `OrderDetailsPreview`: сохранено оформление заголовка строки заказа, новый макет раскрыт по умолчанию. Две позиции с фотографиями из существующего тестового набора, условные цены и получатель; итоги рассчитаны из этих позиций. Кнопки доставки только поясняют, что это макет, не создают этикетки и не меняют статусы. Рабочий `SellerOrderCard` не изменён. Выбор трёх рядов утверждён, конкретное оформление макета ещё примеряем.

Пользователь принял текущую деталку как промежуточный вариант. Количество рядом с позицией показываем только при значении больше 1; «1 шт.» скрыто.

В `/design-system#icons` показаны по три варианта: товар — бирка, куб, ассортимент; заказ — бланк, чек, корзина; передача в доставку — грузовик, посылка в руке, посылка со стрелкой. Семь образов взяты из установленного Lucide, два символа посылки нарисованы для локального предпросмотра. Чёрный контур, 32 px и линия 1,5 используются только для сравнения и не являются утверждёнными глобальными токенами. Общий `Icon` и рабочие экраны не изменены.

После сравнения пользователь выбрал: товар — куб (`Box`, вариант 2), заказ — чек (`ReceiptText`, вариант 2), передача в доставку — грузовик (`Truck`, вариант 1). В эталоне оставлены эти три образа вместо вариантов. Нумерация в ответе пользователя обозначает сущности, а не номера вариантов. Выбор не утверждает автоматически размеры и толщину линий и не разрешает глобальную замену иконок.

Следующим ответом пользователь утвердил размеры: 24 px — навигация и задачи, 20 px — небольшие служебные иконки (стрелки, раскрытие), 32 px — пустые состояния. Контур 1,5, чёрный цвет; цветные иконки только для статусов. Это заменяет прежний временный размер 32 px для всех трёх образов. Правила собраны в локальном `DesignSystemIcon`, применены к галерее, алертам, иконкам деталки и пустым состояниям эталона; общие `Icon` и `EmptyState` не изменены.

Текущий этап: основные решения по кнопкам, полям, статусам, алертам и иконкам зафиксированы в эталоне; деталка временно принята. Проверяем мобильную версию, затем переносим ДС по одному экрану, начиная с главной кабинета продавца, с отдельным просмотром каждого экрана. Новые решения сначала обсуждаем.

Мобильная проверка эталона 9 сентября: на ширинах 360 и 390 px у страницы и её разделов нет горизонтального переполнения. Визуально проверены иконки, поля, кнопки и успешное состояние; выпадающий список категории помещается по ширине, сворачивание и раскрытие заказа работает. Подтверждены размеры 20 / 24 / 32 и линия 1,5; пустое состояние товара использует чёрный куб 32 px, успешное — зелёную галочку 32 px. Это проверка локального эталона, не полный мобильный аудит рабочих страниц. Следующий экран для переноса — главная кабинета продавца.

1. Типографика: роли, размеры, веса, вторичный текст
2. Кнопки: размеры, focus, hover, pressed, disabled, loading и результат действия
3. Поля: подписи, подсказки, ошибки, фокус, недоступность
4. Статусы, алерты и уведомления
5. Сетка, отступы, списки, адаптивность
6. Иконки и анимации
7. Последовательный перенос утверждённых компонентов на рабочие экраны

Согласование фиксируем здесь после ответа пользователя. Массового переноса на рабочие страницы не было; ниже фиксируется первый поэкранный перенос.

## Перенос 1: главная кабинета продавца, 9 сентября 2026

По просьбе пользователя перенесён `SellerHomeTab`, не весь кабинет. В шапке название бренда и принятый `StatusBadge` со словом «Активен», одна основная кнопка добавления товара слева под заголовком. Подготовка — отдельный блок, который скрывается после завершения. Задачи идут перед сводкой магазина. Белый фон, рамки задач и событий как в списке заказов, чёрные подписи и облегчённые пояснения, без декоративных цветных кругов и бледных карточек. Финансовый блок содержит только доступную выплату. Сохранены события и ссылка поддержки из API.

Иконки из эталона вынесены в общий **опционально подключаемый** `components/ui/DesignSystemIcon`: куб, чек и грузовик, размеры 20 / 24 / 32, контур 1,5. Прежний `Icon` не изменён этой миграцией. Для `Button` добавлен опциональный `reserveLabelSpace`, чтобы кнопка создания не сжималась при загрузке и сохраняла доступное имя. `Price` поддерживает опциональное число знаков после запятой: на главной сохранено прежнее округление сумм до рублей. Поведение существующих потребителей без новых опций не меняется.

Условия появления задач, данные и адреса действий сохранены. Проверка готовности, действия создания, авторизация и API не менялись. При отсутствии сводки не показываем ложное отсутствие задач; неизвестный статус магазина не обозначаем активным. Пользовательская правка «текстовый логотип» сохранена. Остальные вкладки, навигационная оболочка, backend и база данных не изменены.

Проверки: `node scripts/check-seller-home.cjs` — работающий магазин, подготовка, загрузка, ошибка, пустые/отсутствующие данные, иконки, ссылки, суммы и состояния кнопки без запросов к БД. ESLint, TypeScript и `git diff --check` прошли. В браузере проверен реальный кабинет после входа пользователя и раскладки 360 / 1280 px. Товары не создавались, действия доставки и выплат не выполнялись. Следующий шаг — просмотр этой главной пользователем, затем перенос следующего экрана.

Дополнение по просьбе пользователя: на главной после сводки и перед событиями добавлен горизонтальный блок «Товары» для быстрого перехода к редактированию. Фото, название и цена, без ID, повторного бренда и статуса на каждой карточке. До 12 активных позиций, ссылка «Все товары», прокрутка свайпом и стрелками на широком экране. Используется только авторизованный seller endpoint, а не публичный каталог. Поскольку API списка пока не фильтрует статус, загрузка продолжает постраничный просмотр до 12 активных позиций либо конца списка, включая случай, когда первая страница состоит из черновиков. Ошибка изолирована в блоке с повтором, загрузка не блокирует главную, запрос отменяется при уходе. Есть скелетон, пустое состояние и замена отсутствующего/недоступного фото. Создание и изменение товаров этим блоком не выполняется.

Блок товаров проверен на реальном кабинете: 12 карточек, переход в редактирование выбранной позиции, прокрутка стрелкой и мобильная раскладка 360 px. Smoke-проверки дополнены фильтрацией статусов, переходом через страницы черновиков, лимитом 12, авторизованным запросом, ошибкой и отменой. ESLint, TypeScript и `git diff --check` проходят.

## Перенос 2: витрина магазина, 9 сентября 2026

По просьбе пользователя перенесены `SellerBrandTab`, фотографии бренда и подборки. Сохранён вертикальный порядок разделов и существующие поля. Убраны номера разделов и серые заливки; предпросмотр логотипа, пустые состояния и строки подборок — белые, с тонкой рамкой как у заказов. Применены общие `TextInput`, `Textarea`, `Button`, `StatusBadge` и `DesignSystemIcon`. Поля и кнопки оформлены локально по принятому эталону, без глобального изменения остальных страниц: подписи 12 px / 400, ввод 14 px / 21 px / 400, чёрная рамка 1 px, пояснения 15 px / 21 px / 300.

«Сохранить» активно до отправки, загрузка показывает только спиннер с сохранением ширины, успешное сохранение — галочку на 1,5 секунды и зелёный алерт без рамки. Форма профиля отделена от фотографии и подборок: форма поиска больше не вложена в форму сохранения. Для создания подборки вместо недоступной кнопки добавлены ошибки названия и выбора товаров с прокруткой и фокусом. Сохранены лимиты, поиск, пагинация, порядок фото и существующие API. Backend, данные и общие компоненты не менялись.

Проверки: `node scripts/check-seller-storefront.cjs`, ESLint, TypeScript, production build и `git diff --check` прошли. В браузере проверены 1280 / 360 px, открытие и закрытие редактора, ошибки пустой подборки, фокус и прокрутка к полю; горизонтального переполнения страницы нет. Сохранение реального профиля, загрузка и удаление файлов/подборок намеренно не выполнялись. Готово к просмотру пользователем.

## Перенос 3: задачи, деталка, общие компоненты и тексты, 9 сентября 2026

Пользователь поручил перенести пять ранее утверждённых пунктов на рабочие экраны. Новые варианты оформления не придумывались.

### Задачи и переходы

- Главная загружает конкретные задачи через существующие авторизованные seller API, с пагинацией, отменой запросов и явной ошибкой при неполной загрузке. Заказы дедуплицируются; сначала показываются ближайшие/просроченные сроки, по восемь задач с продолжением и фильтром типа без счётчиков
- «Новый заказ», «Новый возврат», «Возврат получен», «Товар на доработке» и «Товар заблокирован» имеют отдельные объект, пояснение и действие. Настройка Telegram и другие существующие задачи подготовки сохранены
- Отправка — 72 часа от `paidAt`, включая выходные, без округления до календарных дней. Просрочка выделяется красным. Время оплаты отсутствует — срок не рассчитывается. Задачи отправки исключают неоплаченные, отменённые, завершённые и уже переданные заказы
- «Передать в доставку» ведёт к заказу, но само по себе не меняет статус доставки. Переход открывает нужный заказ даже вне первой страницы списка. Возврат из задачи загружается и получает фокус в подвкладке «Заказы → Возвраты»
- Закрытие задач определяется текущими данными при загрузке главной, не локальной галочкой. Онлайн-уведомления и ответы поддержки не имитируются. При росте объёма данных нужен отдельный серверный endpoint задач вместо сканирования списков товаров и возвратов; сейчас сканирование ограничено с явной ошибкой, а не ложным «задач нет»

### Деталка заказа

Общий `OrderDetailsPanel` подключён к рабочему `SellerOrderCard`: товары → оплата → доставка. Количество видно только при значении больше одного; суммы с ненулевыми копейками не округляются. Загрузка, ошибка и повтор сохранены. Отсутствующие подробности не подменяются выдуманной суммой товаров. Накладная доступна только продавцу; покупательское использование карточки сохранено. Новых финансовых или логистических операций не добавлено.

### Общая ДС

- `Field`, `TextInput`, `Textarea`, `FormSelect`, `FormCombobox`, телефон и нативные размеченные поля используют общие роли: чёрные подписи, единый ввод 14/21/400, чёрный бордер 1 px, ошибка 13/18 рядом с полем и красная рамка по всему периметру. Для `TextInput` и `Textarea` ошибки связаны через `aria-describedby`
- Нативные текстовые поля в профиле, реквизитах, возвратах, карточке товара, фильтрах и административных формах подключены через `data-ui="field"`; радиокнопки, чекбоксы и загрузка файлов не превращались в текстовые поля
- Основной `Button` по умолчанию сохраняет размер при спиннере и успехе. Третий уровень, мягкое нажатие, клавиатурный фокус и reduced-motion унифицированы. Галочка отображается только при явно успешном результате, не автоматически после любого окончания загрузки. Существующие отдельные подтверждения сохранены
- «Сохранить» в реквизитах и профиле больше не блокируется из-за незаполненности или отсутствия изменений; повтор во время запроса блокируется. Общая граница отправки формы прокручивает и фокусирует первое поле с `aria-invalid` / `data-validation-error`, после появления ошибки. Асинхронная серверная валидация и формы вне общего `main` требуют собственных вызовов этого обработчика
- Общие алерты без рамки, бледная семантическая заливка, чёрный текст. Три утверждённые иконки перенесены глобально, контур 1,5; служебные стрелки — 20, пустые состояния — 32. Специализированные иконки сохранили прежнюю семантику, их возможный редизайн требует отдельного согласования
- Счётчики убраны из фильтров заказов и товаров, итоги списка оставлены. Метаданные больше не разделяются точками. Маркеры списков описания товара и маска пароля не являются такими разделителями и сохранены

### Название и содержание

Отображаемое название — «рцмаркет». Главная, футер, метаданные, страницы о проекте и заявки продавца больше не ограничивают описание одеждой и исключительно российским производством: независимые бренды для жизни, дома и подарков, включая предметы искусства. Состав категорий, правила приёма продавцов, API, БД, переменные окружения и реальные артикулы этой правкой не менялись. Упоминание российского номера в телефонной валидации сохранено как действующее техническое ограничение, не правило географии брендов.

### Проверки и границы

Прошли ESLint, TypeScript, production build, `git diff --check`, `check-seller-home.cjs`, `check-seller-storefront.cjs` и новый `check-ds-rollout.cjs`. Новый smoke проверяет точные 72 часа, неизвестную дату оплаты, исключение неподходящих статусов, дедупликацию, пагинацию, авторизацию и отмену запросов, порядок рядов, количество, копейки, покупательские ограничения, поля и иконки. Это frontend-проверки, не аудит backend ownership.

В браузере проверены реальные задачи и фильтр возвратов на 1280 px, переход с главной к конкретному возврату с фокусом, открытие заказа вне первой страницы, три ряда деталки на 360 px, реквизиты и локальная ошибка пустого ФИО с фокусом и красной рамкой. При проверке кнопки сохранения существующие заполненные реквизиты были отправлены без намеренного изменения значений; показано подтверждение сохранения. Затем пустое ФИО проверено без сохранения, форма восстановлена перезагрузкой. Доставки, возвраты денег, товары и файлы не изменялись.

В тестовом заказе №558 обнаружено несоответствие суммы позиций и итоговой суммы из API. Интерфейс выводит присланные значения; тестовые данные и финансовая логика не исправлялись в рамках UI-переноса. Сопоставление сумм требует отдельной проверки источника данных.

Это перенос общей основы, а не заявление о завершённом ручном аудите каждой страницы. Остались поэкранный просмотр всех состояний покупателя/администратора, специализированные иконки и отдельные формы с собственной асинхронной валидацией. Backend не менялся; миграции, коммиты, push и выкладка не выполнялись.

## Перенос 4: рабочие лайки, 9 сентября 2026

После замечания пользователя проверка нестандартных форм приостановлена, без новых правок этих форм. Ранее лайк действительно оставался только в эталоне.

Добавлен общий `LikeButton`, подключён в `ProductTile` (каталог, подборки и избранные товары), деталку товара, страницу бренда, сохранённые бренды и сам эталон. Старые отдельные стили лайков удалены. Квадрат 44 × 44 px без рамки, сердце 24 px; мгновенный фон `--surface-hover`, плавное заполнение чёрным за 220 мс. При снятии лайка сердце сразу становится контурным, фон остаётся до ухода курсора. Повторное заполнение по hover доступно после выхода и нового наведения. Клавиатурный фокус и reduced-motion сохранены; навигационные ссылки с сердцем в шапке не являются лайками и не переделывались в кнопки переключения.

На время запроса локально показывается новое состояние, повторный клик блокируется, затем применяется подтверждённое значение. Сохранение и сообщения об ошибках остаются у прежних обработчиков. У избранных брендов исправлен откат при сетевой ошибке: восстанавливается только затронутый бренд, без отмены параллельного изменения другого бренда и без восстановления состояния прежней сессии.

Проверки: `check-like-button.cjs` — немедленное снятие, сброс hover, блокировка повторов, откат ошибки и независимость брендов, единое подключение пяти потребителей. В браузере проверены выбранное и снятое состояния с серым фоном на безопасном примере, без сохранения реальных лайков. ESLint, TypeScript и `git diff --check` прошли. Коммит и выкладка не выполнялись.

## Перенос 5: формы возвратов и ошибки габаритов, 9 сентября 2026

Возобновлена проверка нестандартных форм. В возвратах выделена `ReturnInspectionForm` с общими `FormSelect`, `TextInput`, `Textarea` и `Button`. Убраны самостоятельные стили полей, внутренняя чёрная разделительная линия и дублирующая подпись пустого списка. Карточки получили ту же тонкую рамку, что строки заказов; пояснения чёрные. Существующие статусы и условия разрешённых действий сохранены.

Ошибки запросов и подтверждения получения/проверки показываются в соответствующей карточке. Загрузка дополнительных возвратов — спиннер без замены текста кнопки, с сохранением размера; ошибки загрузки имеют повтор. Повторные действия блокируются также синхронным ref, до следующего React render.

Сумма проверяется до отправки: пустое значение больше не преобразуется в ноль, некорректное и отрицательное значение получает ошибку под полем с фокусом и прокруткой. Явно введённый ноль сохранён как допустимый ранее вариант; поддержаны точка и запятая. Окончательная допустимость суммы остаётся за backend, его проверки и расчёты не менялись. Комментарий и состояние товара инициализируются из имеющихся данных, а не сбрасываются при подгрузке других карточек.

У веса и габаритов товара добавлено пояснение «Укажите значение больше нуля», связанное через `aria-describedby`. Ошибки названия и описания используют общую роль ошибки, а не стиль чёрного вспомогательного текста. Числовые поля сохраняют прежний ввод и форматирование.

Прошли `check-seller-forms.cjs`, ESLint, TypeScript и `git diff --check`. Smoke проверяет общие поля и загрузку, ошибочную сумму без вызова обработчика отправки, явный ноль, десятичные значения, прокрутку, защиту от двойной отправки и подписи габаритов. В браузере проверена пустая сумма возврата №6: красная рамка, пояснение под полем и фокус; запрос проверки заблокирован локально. Затем форма восстановлена перезагрузкой. Финансовые операции и подтверждение получения не выполнялись. Коммит и выкладка не выполнялись.

## Перенос 6: рабочий редактор товара, 9 сентября 2026

Проверены реально подключённые `ProductGeneralCard` и `ProductVariantsCard`; неиспользуемые старые `ProductImagesCard` и `ProductStickyHeader` не выдаём за рабочие экраны и не переписываем.

- Название, категория, описание, цена, размер и остаток получили пояснения к существующим ошибкам и связи `aria-describedby`. У количества есть явная связь подписи с вводом. Условия ошибок не менялись: цена больше нуля, размер не должен повторяться, остаток неотрицательный
- Описание и состав используют общую типографику полей; красным выделяется вся рамка редактора описания. Для `FormCombobox` добавлены опциональный `errorId`, доступное имя и признак обязательности. Маркеры содержательных списков сохранены
- Выбор фотографий — общий `Button` с сохранением размера: во время загрузки только спиннер. Счётчик загрузки остаётся вне кнопки; прогресс учитывает reduced-motion. При отсутствии обязательного фото есть пояснение и видимая красная рамка. Файловый input сохранён, лимиты и API загрузки не менялись
- «Добавить размер» использует третий уровень кнопок. Крестики удаления фото/размера — существующая иконка `x`, 20 px, контур 1,5; доступ с клавиатуры восстановлен. Условия запрета удаления сохранены
- «Сохранить» не блокируется только из-за отсутствия изменений; сначала выполняется существующая валидация. Запрет редактирования по статусу и блокировка на время операций остаются. Правила отправки на модерацию не менялись. Нижние кнопки используют общую ДС без локальных переопределений
- На 360 px устранено переполнение строки «Цена / Размер / Количество». До 480 px нижние кнопки стоят вертикально, их текст не обрезается; добавлен запас под фиксированную панель. Блок выбора фото на узком экране складывается вертикально

Прошли ESLint, TypeScript, `check-seller-forms.cjs`, `check-ds-rollout.cjs`, `check-seller-storefront.cjs`, `git diff --check`. Smoke проверяет подписи ошибок, их связь с полями, отсутствие видимого текста внутри кнопки загрузки и сохранение скрытой надписи для размера кнопки. В браузере проверен существующий активный товар №390: ограничения редактирования, ввод цены/остатков и мобильная панель действий. Данные не редактировались и не сохранялись, файлы не загружались, публикация и снятие с витрины не выполнялись. Коммит и выкладка не выполнялись.

## Перенос 7: подтверждения и реквизиты, 9 сентября 2026

Добавлен общий `Dialog` на нативном modal dialog: фон недоступен для взаимодействия, фокус возвращается после закрытия, Escape и закрытие блокируются на время запроса. Он используется для архивации/удаления товара вместо `window.confirm` и для результата сохранения реквизитов. Кнопки из ДС, пояснения чёрные 15/21/300, успешный результат с зелёной иконкой. Ошибка операции остаётся внутри открытого окна для повтора. Запросы архивации/удаления и создания следующего товара защищены от повторного клика синхронным ref; правила допустимости статусов и API не менялись.

В существующем окне успешной публикации обновлены вторичная кнопка, иконки и пояснение; его прежнее управление фокусом сохранено. У реквизитов убраны переопределения размеров кнопки сохранения, ошибки текстовых полей связаны с полями через aria-describedby. Устаревшие стили окна реквизитов удалены.

Проверки: ESLint, TypeScript, `check-seller-dialogs.cjs`, `check-seller-forms.cjs`, `check-ds-rollout.cjs`, `git diff --check`. Изолированные проверки покрывают семантику, блокировку Escape при запросе, восстановление прокрутки/фокуса. В браузере открыто и отменено подтверждение снятия товара №390, проверены Escape, возврат фокуса и ширины 599/360 px. Удаление, архивация, сохранение реквизитов и создание товара не выполнялись. Полная production-сборка в этом шаге не запускалась. Коммит и выкладка не выполнялись.

## Товары: выбор и массовые действия, 9 сентября 2026

По запросу пользователя добавлены квадратные чекбоксы слева и меню дополнительных действий справа, независимо от перехода в карточку. При выборе появляется белая плавающая панель по центру снизу: прямые углы, общие кнопки, мягкое появление с учётом reduced-motion. На телефоне панель занимает две строки. «Выбрать все показанные» относится только к отображаемым загруженным товарам; смена фильтра очищает выбор.

Подключены существующие seller API: модерация, архив, возврат из архива в черновики и удаление. Подтверждение через общий `Dialog` заранее показывает подходящие товары и причины пропуска остальных. Запросы последовательные, повторное подтверждение блокируется, успешные товары снимаются с выбора; ошибки показываются отдельно со ссылками на карточки. При сетевой неопределённости автоматического повтора POST нет. Пагинация учитывает сдвиг после удаления. Проверки прав и публикации на backend не менялись; редактор цен/остатков, подборки и экспорт не добавлялись.

Изолированный `check-seller-product-actions.cjs` проверяет разметку, статусы, подтверждения, частичные ошибки, дедупликацию и остановку при потере соединения/сессии. В браузере проверены 1280/360 px, чекбоксы, меню, клавиатура, отмена подтверждения и смена фильтра на временном примере без мутаций; пример удалён после проверки. Реальные товары не архивировались, не удалялись и не отправлялись на модерацию.

Рабочий кабинет также загрузился: проверены выбор ровно 50 показанных товаров из 78 и снятие выделения. Прошли ESLint, TypeScript, production build, новый smoke, `check-seller-dialogs.cjs`, `check-ds-rollout.cjs` и `git diff --check`. Коммит, push и выкладка не выполнялись.

### Уведомления о результатах действий, 10 сентября 2026

Утверждено пользователем: компактные уведомления снизу по центру, прямые углы, без рамки и тени, текст слева. Успех — заливка из `--success` (8% с `--white`), ошибка — из `--danger` (8% с `--white`); текст чёрный, описание с весом 300. Успех исчезает через 4 секунды. Ошибка, требующая исправления, остаётся до закрытия; доступны крестик и secondary-действие с чёрной рамкой. Учитывается reduced-motion.

В списке товаров массовая операция выдаёт одно сообщение с количеством успехов и ошибок. «Посмотреть ошибки» открывает общий Dialog с полным списком причин и переходами. Одиночная ошибка ведёт к товару, а проблемы реквизитов/оферты — в «Данные и документы». Известные причины публикации выводятся конкретно на русском; неизвестные ответы не показывают HTML и служебные данные. Предыдущий блок «Готово: 1 из 1» отменён. Интерактивные примеры доступны в `/design-system#feedback`; кнопка действия в примере не изменяет данные.

### Подтверждение удаления, 10 сентября 2026

Любое действие удаления требует явного подтверждения до изменения данных. Диалог из ДС называет объект и последствия, содержит «Отмена» и «Удалить». Закрытие и Escape ничего не удаляют. Повторная отправка блокируется на время запроса. Для новых действий используется ConfirmActionButton; существующие подтверждения удаления товаров сохраняются. Покрыты подборки продавца и администратора, фото бренда и товара, размеры и корзина. Отключение Telegram и значений справочника тоже требует подтверждения. Переключение лайка не является удалением сущности и сохраняет согласованное поведение.

### Шапка и сортировка товаров, 10 сентября 2026

На широком экране названия колонок вынесены из строк в общую шапку: название, цена, варианты, остаток, статус. Повторный клик переключает возрастание/убывание, стрелка указывает подтверждённый порядок. На узком экране сохранены карточки с подписями значений, сортировка доступна отдельной компактной строкой сверху.

Сортировка выполняется backend до пагинации, а следующая страница запрашивается с тем же порядком. Неограниченный остаток считается выше числового; статусы упорядочены по русским подписям. Сервер принимает только разрешённые поля и добавляет ID для стабильного порядка равных значений. Ошибка запроса сохраняет прежнюю выдачу и сортировку. Для применения изменений API нужен перезапуск локального backend.

Проверены TypeScript, ESLint затронутых файлов, smoke действий/переключения сортировки, unit-тесты backend и интеграционный тест пяти полей в двух направлениях на отдельных PostgreSQL/Redis контейнерах с пагинацией и ownership. Шапка визуально проверена на ширине 1280 px, компактный режим — в текущем узком окне. Реальные товары не изменялись, backend не перезапускался.
# Панели редактирования

Утверждено: редактор товара открывается широкой панелью справа (до 1120 px), создание подборки — компактным окном по центру. На узком экране оба редактора занимают весь экран. Используется общий `EditorSurface`: белый фон, прямые углы, цвета из токенов, крестик справа сверху, независимый скролл формы и закреплённые действия снизу. При несохранённых изменениях закрытие крестиком или Escape требует подтверждения; во время запроса закрытие заблокировано. Анимация открытия учитывает reduced motion. Адрес редактора товара сохраняется; переход из списка не удаляет фоновый список.

Переключатели разделов «Заказы / Возвраты» и «Товары / Подборки» используют единый `CabinetTabs appearance="segmented"` из ДС, без локальных переопределений: серые прямоугольные плашки, более тёмный серый hover, активная вкладка — белый текст на чёрном фоне, без счётчиков и подчёркиваний. Верхнее меню продавца остаётся текстовым.

Верхнее меню продавца: активный пункт имеет вес 500, остальные — 400. Справа от табов «Товары / Подборки» одна основная кнопка: «Создать товар» в товарах и «Создать подборку» в подборках; отдельная кнопка «Новая подборка» удалена; на узком экране переносится на следующую строку. Сохранены состояние загрузки и блокировка повторного нажатия.

Верхние кнопки «Создать товар» и «Создать подборку» всегда сохраняют обычный вид без disabled и смены подписи на спиннер. Повторный запуск отсечён в обработчиках, превышение лимита подборок объясняется по нажатию.

В списке заказов продавца в одной ячейке «Товары» общей строки даты, номера, статуса и суммы видны первые две позиции: миниатюра 48 × 64, название, размер, цвет и количество. «Ещё N товаров» раскрывает остальные позиции независимо от деталки заказа. Полный состав загружается для строк около видимой области через существующий кэш деталей; до загрузки используются названия и первое фото без выдуманных вариантов или количества. Есть повтор при ошибке и заглушка изображения. Покупательские списки не менялись. Проверены 1280/390 px, отсутствие горизонтального переполнения, check-order-products-preview.cjs, TypeScript и ESLint.

Редактор товара: фото загружаются и редактируются в правой колонке вместо отдельного блока в основной форме. Общий ProductPhotoEditor сохраняет выбор нескольких файлов, drag-and-drop, прогресс, ошибки, удаление с подтверждением и перестановку; первое фото крупное, остальные миниатюрами. Добавлены кнопки перестановки для клавиатуры и сенсорного экрана. Ограничения редактирования опубликованных товаров сохранены.

Фото в редакторе переработаны: правая колонка 40% (минимум 380 px на широком экране), просмотр целого снимка без обрезки, одинаковые миниатюры с выбором, единая панель порядка/главного фото/удаления и общая кнопка загрузки. Кнопки поверх каждой миниатюры удалены, использованы контурные иконки и ghost/secondary из ДС. Проверены выбор снимка, мобильная ширина 390 px, TypeScript, ESLint и проверки форм; реальные фото не изменялись.

Редактор товара: по уточнению пользователя «Сохранить» сохраняет неполный черновик без проверки готовности к модерации. Проверка названия, описания, категории, фотографий, габаритов и вариантов запускается только при отправке на модерацию. Кнопка отправки доступна при незаполненной/изменённой форме; текущий статус и готовность магазина проверяются по нажатию. Валидные несохранённые изменения автоматически сохраняются до POST publish. Ошибка сохранения или изменение ревизии останавливают отправку. Во время выполняющейся операции повторные запросы блокируются. Проверено check-product-save-moderation.cjs с подменённым API; реальные товары не отправлялись на модерацию.
